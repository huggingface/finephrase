# Rephrasing benchmark experiment configuration
# Tests different models on 100M tokens

experiment_name: "rephrasing_benchmark_100M_no_thinking"
script: "rephrase"
continue_on_failure: true

# Fixed arguments that apply to all runs
fixed_args:
  # Data and output configuration
  data_paths: "s3://fineweb-data-processing-us-east-1/edu_annotated/score1_2"
  limit: 2_000_000  # On average a document contains 500 tokens, so this will give us ~100M tokens
  
  # Processing configuration
  n_tasks: 2
  records_per_chunk: 500
  
  # Model configuration 
  prompt_template: "dspy/5-max_full_evals.md"  # Adjust prompt as needed
  temperature: 0.6
  top_p: 0.8
  top_k: 20
  presence_penalty: 1.5
  max_tokens: 4096
  model_max_context: 8192
  enable_thinking: false
  
  # Infrastructure configuration
  max_concurrent_requests: 500
  max_concurrent_tasks: 500
  metric_interval: 120
  server_type: "vllm"
  
  # Debugging and checkpoints
  debug: false # Logs get very verbose when debug is true
  tokenizer: "hynky/Llama-3.2-1B-no-bos"
  
  # Slurm configuration
  time: "20:00:00"
  partition: "hopper-prod"
  qos: "normal"
  gpus: 2

# Variable arguments - creates different experiment runs
runs:
  - name: "qwen_0.6b_fp8"
    args:
      model_name_or_path: "Qwen/Qwen3-0.6B-FP8"
  - name: "qwen_1.5b_fp8"
    args:
      model_name_or_path: "Qwen/Qwen2.5-1.5B"
  - name: "qwen_4b_fp8"
    args:
      model_name_or_path: "Qwen/Qwen3-4B-FP8"
  - name: "qwen_8b_fp8"
    args:
      model_name_or_path: "Qwen/Qwen3-8B-FP8"
  - name: "qwen_14b_fp8"
    args:
      model_name_or_path: "Qwen/Qwen3-14B-FP8"
  - name: "qwen_32b_fp8"
    args:
      model_name_or_path: "Qwen/Qwen3-32B-FP8"
